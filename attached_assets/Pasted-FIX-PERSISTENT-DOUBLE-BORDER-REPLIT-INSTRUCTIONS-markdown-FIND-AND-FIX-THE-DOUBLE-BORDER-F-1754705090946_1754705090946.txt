FIX PERSISTENT DOUBLE BORDER:

REPLIT INSTRUCTIONS:
markdown## FIND AND FIX THE DOUBLE BORDER

### FILE: `/client/src/components/Navigation.tsx`

### DEBUGGING STEPS:

1. **SEARCH FOR THE USER BUTTON CODE**
   Look for the User icon import and find where it's used:
Search for: "User" or "ChevronDown" in the file

2. **THE PROBLEM IS LIKELY ONE OF THESE:**

**SCENARIO A - Button inside a bordered div:**
```jsx
// WRONG - Double border
<div className="border border-gray-700/30 rounded-lg">
  <button className="border border-gray-700/30 rounded-lg">
    <User />
  </button>
</div>
SCENARIO B - Padding making it look double:
jsx// WRONG - Extra padding
<button className="p-1 border border-gray-700/30">
  <div className="p-2 border border-gray-700/30">
    <User />
  </div>
</button>

THE FIX - MATCH CART BUTTON EXACTLY:
Find your cart button code:
jsx{/* This is probably working correctly */}
<Link href="/cart">
  <a className="... border border-gray-700/30 rounded-lg ...">
    <ShoppingCart />
  </a>
</Link>
Make user button identical structure:
jsx{/* User button should be EXACTLY like cart */}
<div className="relative"> {/* NO BORDER ON THIS DIV */}
  <button 
    onClick={() => setDropdownOpen(!dropdownOpen)}
    className="flex items-center gap-2 px-3 py-2 
               bg-[#1e293b]/30 border border-gray-700/30 
               rounded-lg hover:bg-[#1e293b]/50 
               transition-all"
  >
    <User className="w-5 h-5 text-gray-400" />
    <ChevronDown className="w-4 h-4 text-gray-400" />
  </button>
  
  {/* Dropdown goes here */}
  {dropdownOpen && (
    <div className="absolute right-0 mt-2 ...">
      {/* dropdown content */}
    </div>
  )}
</div>

QUICK CHECK - Inspect in browser:

Right-click the user button
Inspect Element
Look for any element with border class
Should only be ONE border on the button itself


NUCLEAR OPTION - Replace entire user section:
jsx{/* Delete everything related to user button and replace with: */}
<button 
  onClick={() => setDropdownOpen(!dropdownOpen)}
  className="flex items-center gap-2 px-3 py-2 
             bg-[#1e293b]/30 border border-gray-700/30 
             rounded-lg hover:bg-[#1e293b]/50 
             transition-all"
>
  <User className="w-5 h-5 text-gray-400" />
  <ChevronDown className="w-4 h-4 text-gray-400" />
</button>


KEY POINT:
The user button should have EXACTLY the same classes as the cart button.
No extra wrappers with borders, no nested borders, just one simple button with one border.
Compare these two and make them identical:

Cart: border border-gray-700/30 rounded-lg
User: Should be EXACTLY the same


The issue is there's definitely an extra border somewhere. Check for any wrapper divs or nested elements with border classes!