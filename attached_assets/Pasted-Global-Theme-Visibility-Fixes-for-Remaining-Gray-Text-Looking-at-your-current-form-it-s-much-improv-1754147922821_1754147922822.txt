Global Theme Visibility Fixes for Remaining Gray Text
Looking at your current form, it's much improved! Here are the specific global theme fixes for the remaining low-visibility elements:
1. HELPER/INFORMATIONAL TEXT
Find and update globally:
css/* All helper text that appears gray */
.text-muted, 
.text-gray-500,
.text-gray-600,
p.help-text,
span.help-text,
.form-help {
  @apply text-gray-300 !important;
}

/* Specific helper text patterns */
p:has(> small),
div[class*="help"],
div[class*="info"] {
  @apply text-gray-300;
}
2. FILE UPLOAD SECTION TEXT
Update globally:
css/* Upload area text */
.upload-text,
.dropzone-text,
[class*="upload"] p,
[class*="drag-drop"],
.file-upload-hint {
  @apply text-gray-200 !important;
}

/* "Choose Files" button */
input[type="file"]::file-selector-button,
.file-input-button,
button[class*="file"],
.choose-files-btn {
  @apply bg-gray-700 text-white border border-gray-400 
         px-4 py-2 rounded cursor-pointer
         hover:bg-gray-600 hover:border-gray-300 !important;
}
3. BULLET POINTS / LISTS
Fix all instructional lists:
css/* All list items in forms */
ul li,
ol li,
.form-instructions li,
[class*="instruction"] li,
[class*="requirement"] li {
  @apply text-gray-200 !important;
}

/* Bullet points specifically */
ul li::marker,
ol li::marker {
  @apply text-gray-300;
}
4. GLOBAL CSS OVERRIDES
Add to your main CSS file or globals.css:
css/* Force all secondary text to be more visible */
.text-secondary,
.text-muted-foreground,
[class*="text-gray-"]:not(.text-gray-100):not(.text-gray-200):not(.text-gray-300) {
  @apply text-gray-300 !important;
}

/* Ensure all form-related text is visible */
form p:not(.error):not(.success),
form span:not(.required),
form small,
form .hint,
form .help {
  @apply text-gray-200 !important;
}
5. TAILWIND CONFIG UPDATE
In tailwind.config.js, add semantic color aliases:
javascripttheme: {
  extend: {
    colors: {
      // Semantic colors for forms
      'form-text': {
        primary: '#ffffff',      // white
        secondary: '#e5e7eb',    // gray-200
        muted: '#d1d5db',        // gray-300
        placeholder: '#9ca3af',  // gray-400
      },
      'form-helper': '#e5e7eb',  // gray-200
      'form-instruction': '#d1d5db', // gray-300
    },
    // Override default grays in form context
    textColor: {
      DEFAULT: '#ffffff',
      muted: '#d1d5db',
      secondary: '#e5e7eb',
    }
  }
}
6. COMPONENT-LEVEL FIXES
For any remaining issues, add these utility classes:
css/* Utility classes for quick fixes */
.readable-text {
  @apply text-gray-200 !important;
}

.readable-helper {
  @apply text-gray-300 !important;
}

.readable-placeholder::placeholder {
  @apply text-gray-400 !important;
}
7. SPECIFIC FIXES FOR YOUR FORM
Based on what I see, update these specific elements:
css/* "This helps us make a fair offer..." text */
.price-helper-text,
[class*="fair-offer"],
.form-note {
  @apply text-gray-200 text-sm;
}

/* Upload instructions */
.upload-instructions,
.photo-requirements {
  @apply text-gray-200;
}

/* File input area */
.file-drop-zone {
  @apply border-2 border-dashed border-gray-500 
         hover:border-gray-400 hover:bg-gray-800/30;
}
8. POSTCSS OR SASS GLOBAL RULE
If using PostCSS or Sass, add this catch-all:
scss// Catch any gray text we missed
@layer utilities {
  form {
    [class*="gray-5"],
    [class*="gray-6"],
    [class*="gray-7"] {
      @apply text-gray-300 !important;
    }
  }
}
These global theme fixes will ensure:

All helper text is gray-200 or gray-300 (much more visible)
File upload areas have better contrast
Instructional text and bullets are clearly readable
The UI maintains its current look while fixing visibility issues
Future forms will automatically inherit these improvements