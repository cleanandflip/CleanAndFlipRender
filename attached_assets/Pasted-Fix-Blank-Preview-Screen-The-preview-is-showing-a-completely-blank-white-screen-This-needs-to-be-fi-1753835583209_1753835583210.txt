Fix Blank Preview Screen
The preview is showing a completely blank white screen. This needs to be fixed so I can see the application.
Diagnostic Steps & Fixes Needed:
1. Check Server Status
Look at the console output and verify:

Is the server actually running? (Should see "serving on port 5000" or similar)
Are there any error messages preventing startup?
Is the app crashing immediately after starting?

2. Verify Port Configuration
The preview might be looking at the wrong port:
javascript// Check these files for port mismatches:
// server/index.ts - backend port (usually 5000)
// vite.config.js - frontend dev server port (usually 5173)
// package.json - check the "dev" script
3. Fix Vite Configuration
If using Vite, update vite.config.js:
javascriptexport default {
  server: {
    host: '0.0.0.0',
    port: 5173,
    strictPort: true,
    hmr: {
      clientPort: 443
    }
  }
}
4. Check Replit Configuration
Ensure .replit file has correct run command:
tomlrun = "npm run dev"
language = "nodejs"

[env]
PATH = "/home/runner/$REPL_SLUG/.config/npm/node_global/bin:/home/runner/$REPL_SLUG/node_modules/.bin"

[[ports]]
localPort = 5173
externalPort = 80
5. Update Package.json Scripts
Make sure the dev script runs both frontend and backend:
json{
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "tsx server/index.ts",
    "client": "vite",
    "preview": "vite preview"
  }
}
6. Check for Build Errors

Look for TypeScript errors
Check for missing dependencies
Verify all imports are correct
Make sure index.html exists in the right location

7. Verify File Structure
Ensure these files exist:

/index.html (root level for Vite)
/src/main.tsx or /src/index.tsx
/server/index.ts

8. Add Basic HTML
If index.html is missing or incorrect:
html<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Clean & Flip</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>
9. Install Missing Dependencies
If using Vite + React:
bashnpm install --save-dev vite @vitejs/plugin-react
npm install react react-dom
10. Clear Cache & Restart

Stop the current process (Ctrl+C)
Clear npm cache: npm cache clean --force
Delete node_modules and package-lock.json
Run npm install
Run npm run dev

Please diagnose why the preview is blank and implement the necessary fixes to get the application displaying properly.