Here's a prompt for Replit:

URGENT: Product Cards Still Too Cluttered - Need Minimal Clean Design
Current Issues:

Too many badges (Featured + Only 1 left + Condition)
Visible "Add to wishlist" text cluttering cards
Long descriptions making cards cramped
Too much information competing for attention

MINIMAL CLEAN CARD DESIGN:
javascript// components/ProductCard.jsx - ULTRA CLEAN VERSION
const ProductCard = ({ product }) => {
  return (
    <div className="group relative bg-gray-800/30 rounded-lg overflow-hidden hover:shadow-xl transition-all">
      {/* Only show critical badge */}
      {product.stock === 1 && (
        <div className="absolute top-3 left-3 z-10">
          <span className="bg-red-500 text-white px-2 py-1 rounded text-xs">
            Only 1 left
          </span>
        </div>
      )}
      
      <Link href={`/products/${product.id}`}>
        {/* Clean Image */}
        <div className="aspect-square relative bg-gray-900/30">
          {product.images?.length > 0 ? (
            <img 
              src={product.images[0]} 
              alt={product.name}
              className="w-full h-full object-cover"
            />
          ) : (
            <div className="flex items-center justify-center h-full">
              <Package className="w-16 h-16 text-gray-700" />
            </div>
          )}
        </div>
        
        {/* Minimal Info Section */}
        <div className="p-4">
          {/* Title */}
          <h3 className="font-medium text-white mb-1 line-clamp-1">
            {product.name}
          </h3>
          
          {/* Brand */}
          <p className="text-gray-500 text-sm mb-3">
            {product.brand}
          </p>
          
          {/* Price Only */}
          <p className="text-2xl font-bold text-white">
            ${product.price}
          </p>
        </div>
      </Link>
      
      {/* Hover Actions - Subtle */}
      <div className="absolute inset-0 bg-black/0 group-hover:bg-black/40 transition-all flex items-center justify-center opacity-0 group-hover:opacity-100">
        <div className="transform translate-y-4 group-hover:translate-y-0 transition-transform">
          <button 
            onClick={(e) => {
              e.preventDefault();
              // Add to cart logic
            }}
            className="bg-white text-black px-6 py-2 rounded-lg font-medium hover:bg-gray-100"
          >
            Add to Cart
          </button>
        </div>
      </div>
      
      {/* Subtle Wishlist - Icon Only */}
      <button 
        className="absolute top-3 right-3 w-8 h-8 bg-white/10 backdrop-blur rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity"
        onClick={(e) => {
          e.preventDefault();
          // Wishlist logic
        }}
      >
        <Heart className="w-4 h-4 text-white" />
      </button>
    </div>
  );
};
REMOVE/HIDE:

❌ "Featured" badge (unless absolutely critical)
❌ Condition badges (GOOD, LIKE NEW) from cards
❌ Weight/specs from cards
❌ Long descriptions
❌ "Add to wishlist" text
❌ Multiple action buttons visible at once

CLEAN GRID LAYOUT:
javascript// Products page grid
<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 p-6">
  {products.map(product => (
    <ProductCard key={product.id} product={product} />
  ))}
</div>
DESIGN PRINCIPLES:

Minimal Information: Name, Brand, Price - that's it
Single Badge: Only show "Only 1 left" when critical
Clean Hover: Simple overlay with one CTA
No Clutter: Remove all non-essential elements
Breathing Room: Proper spacing and padding

RESULT:

Clean, scannable cards
Focus on product image and price
Details saved for product page
Professional, minimal aesthetic

The cards should look like a premium e-commerce site, not a cluttered marketplace!