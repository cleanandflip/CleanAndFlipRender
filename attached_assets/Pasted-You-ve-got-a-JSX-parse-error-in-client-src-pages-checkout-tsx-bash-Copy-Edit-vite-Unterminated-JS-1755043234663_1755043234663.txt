You’ve got a JSX parse error in client/src/pages/checkout.tsx:

bash
Copy
Edit
vite: Unterminated JSX contents (551:12)
549 |           </aside>
550 |         </div>
551 |       </div>   ^
This almost always means either (a) a JSX comment written as a plain JS block comment or // inside JSX, or (b) mismatched tags near the end of the return.

Here’s a precise “do-this-now” fix for Replit.

Replit — fix the checkout JSX error
Wrap JSX comments correctly

In client/src/pages/checkout.tsx, search within the component’s returned JSX for any of these:

/* @ts-expect-error */

/* ... */

// ...

Replace each with proper JSX comments inside braces:

tsx
Copy
Edit
{/* @ts-expect-error */}
{/* any comment text */}
(Plain /* ... */ or // inside JSX will trigger “Unterminated JSX contents”.)

Balance closing tags at the bottom of the component

The bottom of the component should end exactly like this (one wrapper <div> around <form> and <aside>):

tsx
Copy
Edit
return (
  <div className="grid grid-cols-1 xl:grid-cols-[1fr_400px] gap-6">
    <form> {/* ...form contents... */} </form>

    <aside className="p-6 rounded-2xl border-2 border-white/15">
      {/* ...summary contents... */}
    </aside>
  </div>
);
}
Ensure there is only one </div> after </aside>.
If you currently have:

bash
Copy
Edit
</aside>
</div>
</div>
delete the extra </div> so it matches the structure above.

If you added any extra wrapper around <aside>, make sure it has a matching close tag (and keep the total wrappers balanced).

Save & re-run

Save the file and run dev again. The Vite error should disappear.

Optional guardrails (helps prevent this again)
If you left the TypeScript directive comment near JSX, keep it as a JSX comment:

tsx
Copy
Edit
{/* @ts-expect-error – reason */}
{isAuthenticated && ( /* ... */ )}
Add ESLint rules (if you use ESLint):

json
Copy
Edit
"rules": {
  "react/jsx-no-comment-textnodes": "error",
  "react/jsx-no-undef": "error"
}
If the error persists after these changes, paste the final 60 lines of checkout.tsx and I’ll pinpoint the exact spot.