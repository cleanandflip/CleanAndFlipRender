Tell Replit to Apply Consistent Button Theme Site-Wide
Instructions for Replit:
"You need to standardize ALL buttons across the entire Clean & Flip site to match the button theme shown. Here's what needs to be fixed:
1. Identify the Standard Button Theme
The correct button theme uses:

Primary buttons: Blue background (#3B82F6), white text, hover darkens to 90% opacity
Ghost buttons: Transparent background, hover adds subtle accent background
Glass morphism wrapper: backdrop-filter: blur(12px) with glass background
Animations: 300ms transitions with ease-in-out, subtle lift on hover (-translate-y-0.5)
Focus states: Blue ring with 2px width and offset

2. Fix These Specific Button Groups
Looking at the screenshots, fix:
User Dropdown Menu (Image 1):

My Dashboard, Order History, My Submissions buttons need consistent styling
Apply ghost variant with proper hover states
Add consistent padding and icon spacing

Dashboard Tab Buttons (Image 3):

Orders, Submissions, Wishlist, Profile, Addresses tabs
Active tab should use primary variant (blue)
Inactive tabs should use ghost variant
Add glass morphism container if missing

Admin Dashboard Tabs (Image 4):

Products, Categories, Submissions, Analytics, Wishlist, Users, System tabs
Same active/inactive styling as user dashboard
Ensure consistent spacing and transitions

3. Create/Update Button Component
typescript// Ensure all buttons use this structure:
<div className="flex items-center glass rounded-lg p-1">
  <Button
    variant={isActive ? 'primary' : 'ghost'}
    size="sm"
    className="h-8 transition-all duration-300"
  >
    <Icon className="w-4 h-4 mr-2" />
    {label}
  </Button>
</div>
4. Apply Glass Morphism CSS
Make sure these styles exist globally:
css.glass {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.glass-hover {
  transition: all 300ms ease-in-out;
}

.glass-hover:hover {
  transform: translateY(-2px);
  background: rgba(255, 255, 255, 0.08);
}
5. Update Button Variants
Ensure button.tsx has:
typescriptprimary: {
  bg: '#3B82F6',
  color: '#FFFFFF',
  hover: 'hover:bg-primary/90',
  focus: 'focus-visible:ring-2 focus-visible:ring-blue-500'
}

ghost: {
  bg: 'transparent',
  hover: 'hover:bg-accent hover:text-accent-foreground',
  focus: 'focus-visible:ring-2 focus-visible:ring-ring'
}
6. Fix All Instances
Search and update ALL button instances:

Navigation dropdowns
Dashboard tabs
Form buttons
Modal buttons
Admin panel buttons
Any custom button implementations

7. Consistent Icon Sizing
All button icons should be:

Size: w-4 h-4
Margin: mr-2 when text follows
Color: Inherit from button text color

8. Test These Specific Areas
Verify consistency on:

 User dropdown menu - all options styled consistently
 User dashboard tabs - active/inactive states
 Admin dashboard tabs - active/inactive states
 Add Product button (blue primary)
 Filter/Sort buttons
 All form submit buttons
 Modal confirm/cancel buttons

9. Animation Consistency
Every button should have:

Transition: transition-all duration-300 ease-in-out
Hover lift: hover:-translate-y-0.5
Focus ring: Consistent blue color with 2px width
Active scale: active:scale-95

The goal is EVERY button looks and behaves identically with smooth animations and consistent blue theme."
Key Points for Replit:
"Focus on:

Blue (#3B82F6) for all primary/active buttons
Glass morphism effects on button containers where appropriate
300ms smooth transitions on everything
Consistent padding, sizing, and icon spacing
Remove any conflicting or legacy button styles"