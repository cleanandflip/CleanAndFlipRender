Let’s add the subtle hover/animation you want and make the nav user profile dropdown match the new style exactly.

Below are surgical edits Replit can apply.

1) Dropdown item hover + micro-animation
File: src/components/ui/Dropdown.tsx

Find the item button class (inside the map over options) and replace its className with this:

tsx
Copy
Edit
className={[
  "w-full text-left px-3 py-2.5 flex items-center gap-2 rounded-md select-none",
  // subtle hover color + micro motion
  "transition-[background,transform,opacity] duration-150 ease-out",
  "hover:bg-muted/60 hover:translate-x-[1px]",                    // tiny glide
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand/60",
  opt.disabled ? "opacity-50 cursor-not-allowed" : "cursor-pointer"
].join(" ")}
Add reduced-motion guard (top of the file or global CSS):

css
Copy
Edit
@media (prefers-reduced-motion: reduce) {
  .hover\:translate-x-\[1px\] { transform: none !important; }
  .transition-\[background,transform,opacity] { transition: background-color .15s ease-out; }
}
Result: items get a very soft tint on hover and a 1px glide as the mouse moves, without being flashy.

2) Trigger hover polish (the button you click to open)
Still in Dropdown.tsx, update the triggerCls array to include a gentle hover:

ts
Copy
Edit
const triggerCls = [
  "inline-flex items-center justify-between rounded-lg border",
  "bg-surface/90 border-surfaceBorder text-foreground",
  "hover:bg-surface/95",                                // subtle on hover
  "focus:outline-none focus:ring-2 focus:ring-brand/60 focus:border-transparent",
  "transition-colors disabled:opacity-60 disabled:cursor-not-allowed",
  sizeCls, fullWidth ? "w-full" : "w-[min(90vw,320px)]", className
].join(" ");
3) Make the nav user profile dropdown match the new menu
Swap the current header profile menu to use the same panel style, spacing, motion, and z-index.

File: src/components/ui/Menu.tsx (or your header menu component)

Panel container classes (replace existing):

tsx
Copy
Edit
className="rounded-xl border bg-popover text-popover-foreground shadow-xl p-2 z-[60] w-72 transition duration-200"
Item classes (match dropdown items):

tsx
Copy
Edit
className={[
  "w-full text-left flex items-center gap-2 px-3 py-2 rounded-md select-none",
  "transition-[background,transform,opacity] duration-150 ease-out",
  "hover:bg-muted/60 hover:translate-x-[1px]",
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand/60"
].join(" ")}
Dividers: className="my-2 h-px bg-border/60"

Header block (avatar/name/role): place inside the same panel with className="px-3 py-2.5 rounded-lg bg-surface/60 mb-2" so it’s less transparent and easier on eyes.

Ensure the menu renders via portal to document.body and uses z-index 60 (same as Dropdown) so it never sits behind header or cards.

4) Keep everything consistent via tokens
Use your existing tokens/utilities (no hardcoded colors):

Surfaces: bg-popover, bg-surface, bg-muted/60

Borders: border, border-surfaceBorder, bg-border/60

Text: text-popover-foreground

Focus: focus:ring-brand/60

5) Quick QA checklist
 Dropdown items show soft tint + 1px glide on hover; no jitter.

 Focus ring appears with keyboard (Tab) on both items and triggers.

 prefers-reduced-motion disables the glide (only color transition remains).

 Profile menu panel is less transparent, same radius/shadow as Dropdown.

 Profile menu items have the same hover/motion as Dropdown items.

 Menus render above everything (z-[60]) and never clip.

 No legacy menu styles left in header (grep to confirm).

bash
Copy
Edit
rg -n "hover:bg-.*different|old-menu-class|opacity-[23]\d%" src/components/layout