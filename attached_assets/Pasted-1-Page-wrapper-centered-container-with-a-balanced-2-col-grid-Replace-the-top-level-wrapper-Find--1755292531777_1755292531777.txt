1) Page wrapper → centered container with a balanced 2-col grid
Replace the top-level wrapper

Find:

return (
  <div className="min-h-screen flex">


Replace with:

return (
  <main className="mx-auto max-w-6xl px-4 md:px-6 py-12 md:py-20">
    <div className="grid grid-cols-1 lg:grid-cols-12 lg:items-center lg:gap-12 min-h-[calc(100vh-72px)]">


Find the very last closing tag:

  );
}


Before it, insert a closing </div> and replace the opening <div>’s closing with </main>:

    </div>
  </main>
);
}


Result: the page is constrained to max-w-6xl, padded, and vertically centered under the navbar.

2) Left column (form) → compact, consistent rhythm
Convert the left wrapper into a grid column

Find:

{/* Left Side - Form */}
<div className="flex-1 flex items-center justify-center p-8 lg:p-12">
  <div className="w-full max-w-xl" ref={formContainerRef}>


Replace with:

{/* Left Side - Form */}
<section className="lg:col-span-5 mx-auto w-full max-w-md">
  <div className="w-full" ref={formContainerRef}>


…and find its closing pair and replace:

</div>
</section>

Logo spacing (slightly tighter)

Find:

<Logo size="lg" textOnly={true} className="mb-8" />


Replace with:

<Logo size="lg" textOnly={true} className="mb-6" />

Tabs (shorter, more compact)

Find:

<TabsList className="grid w-full grid-cols-2 mb-8 glass h-14 text-lg">


Replace with:

<TabsList className="grid w-full grid-cols-2 mb-6 glass h-10 md:h-12 text-base">


Find both TabsTrigger lines with text-lg and remove the size, leaving them compact:

className="data-[state=active]:bg-accent-blue data-[state=active]:text-white font-medium transition-all duration-200"

Card padding (denser)

Find both:

<Card className="p-8">


Replace with:

<Card className="p-6 md:p-8">

Login form: labels/inputs/buttons height only

Labels (reduce size, keep color):

Find (in login form):

<Label ... className="text-white font-medium text-xl">


Replace with:

<Label ... className="text-white font-medium text-xs">


Inputs (reduce height & text size):

Find:

className="field h-20 text-2xl px-8"


Replace with:

className="field h-11 text-sm px-3"


Forgot link container: keep as-is (already compact).

Sign In button (match input height):

Find:

className="w-full h-16 text-xl"


Replace with:

className="w-full h-11 text-sm"


“Or continue with” spacing:

Find:

<div className="mt-6">
  <div className="text-center text-sm text-gray-400 mb-3">


Replace with:

<div className="mt-5">
  <div className="text-center text-sm text-gray-400 mb-2">

Register form: make field heights consistent

Name fields:

Find:

className="field h-12 px-4"


Replace with:

className="field h-11 px-3"


Email/Phone (same replacement as above).

Password/Confirm Password:

Find each PasswordInput with:

className="h-12 px-4 ...


Replace with:

className="h-11 px-3 ...


Help text under password (leave text; no change needed).

Create Account button:

Find:

className="w-full h-12 text-lg mt-6"


Replace with:

className="w-full h-11 text-sm mt-5"


Register “continue with” section:

Find:

<div className="mt-6">
  <div className="text-center text-sm text-gray-400 mb-3">


Replace with:

<div className="mt-5">
  <div className="text-center text-sm text-gray-400 mb-2">


Security footer:

Find:

<div className="mt-6 pt-4 ...


Replace with:

<div className="mt-5 pt-4 ...

3) Right column (hero) → balanced to match the form
Convert right wrapper into grid column and remove extra padding

Find:

{/* Right Side - Hero Section */}
<div className="hidden lg:flex flex-1 items-center justify-center p-12">


Replace with:

{/* Right Side - Hero Section */}
<aside className="hidden lg:flex lg:col-span-7 items-center justify-center">


…and find its closing pair and replace:

</aside>

Hero header spacing (slightly tighter)

Find:

<div className="text-center mb-12">
  <div className="inline-flex items-center justify-center w-24 h-24 rounded-full glass mb-8 ...


Replace with:

<div className="text-center mb-10">
  <div className="inline-flex items-center justify-center w-20 h-20 rounded-full glass mb-6 ...


Keep the title text exactly; just adjust size if desired to balance with the narrower layout. If you want to keep text-6xl, that’s fine—no copy changes.

Feature cards: reduce padding & overall gap

Find:

<div className="space-y-8">
  <Card className="p-8 ...


Replace with:

<div className="space-y-6">
  <Card className="p-6 md:p-7 ...


Inside each feature:

Find:

<div className="flex items-start space-x-6">
  <div className="w-16 h-16 ...
  <h3 className="font-bebas text-2xl ...
  <p className="text-text-secondary text-lg">


Replace with:

<div className="flex items-start space-x-5">
  <div className="w-14 h-14 ...
  <h3 className="font-bebas text-2xl ...
  <p className="text-text-secondary text-base">


Icons and headings stay the same; we only reduce paddings/gaps/heights.

4) Global rhythm you’ve now enforced

Outer layout: max-w-6xl + px-4 md:px-6 + py-12 md:py-20.

Grid: lg:grid-cols-12 with left 5 / right 7 columns.

Form elements: h-11, text-sm, px-3, labels text-xs.

Buttons: h-11, text-sm, widths unchanged.

Vertical spacing: prefer mb-6/mt-6 → now mb-5/6 where needed; space-y-6 in forms; space-y-6 for hero cards.

5) Quick acceptance checklist (no visual content changes)

The page is centered and narrower; no edge-to-edge stretching.

On desktop, the form feels compact (no giant inputs); hero balances visually.

Buttons and inputs share the same height.

There are no large dead zones above/below the form.

All wording, headings, icons, and colors are unchanged.