Global UI Style Enhancement Instructions for Replit
1. PRODUCT CARD REDESIGN
css/* Enhanced product cards with depth and polish */
.product-card {
  @apply bg-gray-900/40 backdrop-blur-sm rounded-xl overflow-hidden
         border border-gray-800/50 
         transition-all duration-300 ease-out
         hover:transform hover:translateY(-4px)
         hover:shadow-2xl hover:shadow-blue-500/10
         hover:border-gray-700;
}

/* Card image container */
.product-image-container {
  @apply relative overflow-hidden bg-gray-800/50;
  aspect-ratio: 1/1;
}

.product-image {
  @apply w-full h-full object-cover 
         transition-transform duration-500 ease-out
         hover:scale-110;
}

/* Image overlay gradient */
.product-image-container::after {
  content: '';
  @apply absolute inset-0 
         bg-gradient-to-t from-gray-900/50 to-transparent
         opacity-0 transition-opacity duration-300;
}

.product-card:hover .product-image-container::after {
  @apply opacity-100;
}
2. OUT OF STOCK BADGE REFINEMENT
css/* Softer, more elegant stock badges */
.out-of-stock-badge {
  @apply absolute top-3 right-3 z-10
         bg-red-500/20 backdrop-blur-md
         text-red-300 text-xs font-medium
         px-3 py-1.5 rounded-full
         border border-red-500/30
         shadow-lg shadow-red-500/20;
}

.in-stock-badge {
  @apply absolute top-3 right-3 z-10
         bg-green-500/20 backdrop-blur-md
         text-green-300 text-xs font-medium
         px-3 py-1.5 rounded-full
         border border-green-500/30
         shadow-lg shadow-green-500/20;
}
3. WISHLIST HEART ENHANCEMENT
css/* Refined wishlist button */
.wishlist-button {
  @apply absolute top-3 left-3 z-10
         w-10 h-10 rounded-full
         bg-gray-900/80 backdrop-blur-sm
         border border-gray-700/50
         flex items-center justify-center
         transition-all duration-300
         hover:bg-gray-800 hover:border-gray-600
         hover:shadow-lg hover:scale-110;
}

.wishlist-button svg {
  @apply w-5 h-5 text-gray-400 
         transition-colors duration-300;
}

.wishlist-button:hover svg {
  @apply text-white;
}

.wishlist-button.active {
  @apply bg-red-500/20 border-red-500/50;
}

.wishlist-button.active svg {
  @apply text-red-400 fill-red-400;
}
4. PRODUCT INFO SECTION
css/* Product details styling */
.product-info {
  @apply p-5 space-y-3;
}

.product-title {
  @apply text-lg font-semibold text-white 
         line-clamp-2 leading-snug
         group-hover:text-blue-400 
         transition-colors duration-300;
}

.product-brand {
  @apply text-sm text-gray-400 font-medium;
}

.product-price-container {
  @apply flex items-baseline gap-2 mt-3;
}

.product-price {
  @apply text-2xl font-bold text-white;
}

.product-original-price {
  @apply text-lg text-gray-500 line-through;
}

.product-discount-badge {
  @apply text-xs font-medium text-green-400 
         bg-green-500/20 px-2 py-0.5 rounded-full;
}
5. BUTTON STYLING OVERHAUL
css/* Primary action buttons */
.btn-add-to-cart {
  @apply w-full mt-4 py-3 px-4
         bg-blue-500 hover:bg-blue-600
         text-white font-semibold
         rounded-lg
         transition-all duration-300
         transform hover:scale-105
         shadow-lg shadow-blue-500/25
         hover:shadow-xl hover:shadow-blue-500/40
         disabled:opacity-50 disabled:cursor-not-allowed
         disabled:transform-none disabled:shadow-none;
}

/* Out of stock button state */
.btn-out-of-stock {
  @apply w-full mt-4 py-3 px-4
         bg-gray-800 text-gray-500
         font-semibold rounded-lg
         cursor-not-allowed
         border border-gray-700;
}
6. SEARCH & FILTER BAR ENHANCEMENT
css/* Search input styling */
.search-input {
  @apply w-full px-4 py-3 pl-12
         bg-gray-900/50 backdrop-blur-sm
         border border-gray-700/50
         rounded-xl
         text-white placeholder-gray-500
         focus:bg-gray-900/80
         focus:border-blue-500/50
         focus:outline-none focus:ring-2 
         focus:ring-blue-500/20
         transition-all duration-300;
}

/* Filter button */
.filter-button {
  @apply flex items-center gap-2 
         px-4 py-3
         bg-gray-900/50 backdrop-blur-sm
         border border-gray-700/50
         rounded-xl
         text-white font-medium
         hover:bg-gray-800/50 hover:border-gray-600
         transition-all duration-300;
}
7. GRID LAYOUT IMPROVEMENTS
css/* Product grid container */
.products-grid {
  @apply grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4
         gap-6 lg:gap-8
         px-4 lg:px-0;
}

/* Results count */
.results-count {
  @apply text-sm text-gray-400 font-medium
         mb-6;
}
8. PAGE HEADER ENHANCEMENT
css/* Page title section */
.page-header {
  @apply mb-12 text-center lg:text-left;
}

.page-title {
  @apply text-4xl lg:text-5xl font-bold text-white mb-4
         bg-gradient-to-r from-white to-gray-400
         bg-clip-text text-transparent;
}

.page-subtitle {
  @apply text-lg text-gray-400 max-w-2xl;
}
9. LOADING & EMPTY STATES
css/* Loading skeleton */
.skeleton-card {
  @apply bg-gray-900/40 rounded-xl overflow-hidden
         border border-gray-800/50 animate-pulse;
}

.skeleton-image {
  @apply w-full aspect-square bg-gray-800/50;
}

.skeleton-text {
  @apply h-4 bg-gray-800/50 rounded-full mb-2;
}

/* Empty state */
.empty-state {
  @apply text-center py-20;
}

.empty-state-icon {
  @apply w-24 h-24 mx-auto mb-6 text-gray-700;
}

.empty-state-text {
  @apply text-xl text-gray-400 font-medium mb-2;
}
10. MICRO-INTERACTIONS
css/* Subtle animations */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.product-card {
  animation: fadeInUp 0.5s ease-out;
  animation-fill-mode: backwards;
}

.product-card:nth-child(1) { animation-delay: 0.1s; }
.product-card:nth-child(2) { animation-delay: 0.2s; }
.product-card:nth-child(3) { animation-delay: 0.3s; }
.product-card:nth-child(4) { animation-delay: 0.4s; }

/* Hover effects */
.product-card::before {
  content: '';
  @apply absolute inset-0 
         bg-gradient-to-r from-blue-500/0 to-purple-500/0
         opacity-0 transition-opacity duration-500;
  z-index: -1;
}

.product-card:hover::before {
  @apply from-blue-500/10 to-purple-500/10 opacity-100;
}
11. RESPONSIVE REFINEMENTS
css/* Mobile optimizations */
@media (max-width: 640px) {
  .product-card {
    @apply shadow-lg;
  }
  
  .product-title {
    @apply text-base;
  }
  
  .product-price {
    @apply text-xl;
  }
  
  .btn-add-to-cart {
    @apply py-2.5 text-sm;
  }
}
12. DARK MODE COLOR PALETTE
css:root {
  /* Refined color system */
  --bg-primary: #0a0a0a;
  --bg-secondary: #141414;
  --bg-tertiary: #1f1f1f;
  --border-primary: rgba(255, 255, 255, 0.1);
  --border-secondary: rgba(255, 255, 255, 0.05);
  --text-primary: #ffffff;
  --text-secondary: #a0a0a0;
  --text-tertiary: #6b6b6b;
  --accent-blue: #3b82f6;
  --accent-green: #10b981;
  --accent-red: #ef4444;
}
IMPLEMENTATION NOTES:

Add smooth transitions to all interactive elements
Use backdrop-blur for modern glass effects
Implement subtle shadows with color tints
Add hover states that feel responsive
Use gradient overlays sparingly for depth
Ensure all text has proper contrast
Add loading states for better UX
Use animation delays for staggered effects

These changes will transform the shop page into a modern, polished interface with excellent visual hierarchy and user experience!