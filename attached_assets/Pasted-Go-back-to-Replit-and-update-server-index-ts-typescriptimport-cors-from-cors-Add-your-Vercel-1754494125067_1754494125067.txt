Go back to Replit and update server/index.ts:
typescriptimport cors from 'cors';

// Add your Vercel URL to CORS
const corsOptions = {
  origin: [
    'https://clean-and-flip.vercel.app',  // ← Your Vercel URL
    'https://clean-and-flip-*.vercel.app', // Preview URLs
    'https://cleanandflip.com',  // Your domain (later)
    'http://localhost:5173'
  ],
  credentials: true
};

app.use(cors(corsOptions));
STEP 3: Update Frontend API Calls
In Replit, create client/src/lib/api.ts:
typescript// This will use the VITE_API_URL you just set
const API_URL = import.meta.env.VITE_API_URL || '';

export const api = {
  get: async (path: string) => {
    const response = await fetch(`${API_URL}/api${path}`, {
      credentials: 'include'
    });
    return response.json();
  },
  
  post: async (path: string, data: any) => {
    const response = await fetch(`${API_URL}/api${path}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      credentials: 'include',
      body: JSON.stringify(data)
    });
    return response.json();
  }
};
STEP 4: Fix Any API Call Issues
Search and replace in your frontend code:
typescript// Find all:
fetch('/api/

// Replace with:
fetch(`${import.meta.env.VITE_API_URL}/api/
STEP 5: Push Changes to GitHub
After making changes in Replit:
bashgit add .
git commit -m "Update API configuration for Vercel deployment"
git push
Vercel will auto-redeploy!
STEP 6: Test Your Deployment
Once deployed, visit:
https://clean-and-flip.vercel.app
Test critical functions:

Homepage loads ✓
Products display ✓
Login/Register works ✓
Password reset works ✓


🚨 COMMON FIRST DEPLOYMENT ISSUES:
If Build Fails:

Check the error in Vercel logs
Usually missing dependencies in client/package.json
Or TypeScript errors

If API Calls Fail (CORS):

Make sure Replit backend has CORS configured
Install cors: npm install cors in Replit

If Page is Blank:

Check browser console for errors
Usually means API_URL is wrong


🎯 QUICK FIX COMMANDS:
If you need to update environment variables after deploy:
bash# In Vercel Dashboard:
Settings → Environment Variables → Edit

# Or via CLI:
vercel env add VITE_API_URL production
# Enter: https://CleanAndFlip.cleanandflipyt.repl.co
Click Deploy now and let me know when it's done or if you see any errors!