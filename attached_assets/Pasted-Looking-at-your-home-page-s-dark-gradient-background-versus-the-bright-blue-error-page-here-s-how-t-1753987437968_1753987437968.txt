Looking at your home page's dark gradient background versus the bright blue error page, here's how to update your error pages to match:
Update Error Page Background to Match Home Page
In src/components/error-boundary.tsx, replace the background styling:
tsxexport function ErrorBoundary() {
  // ... existing code ...

  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-900 via-gray-800 to-gray-900 flex items-center justify-center px-4">
      <div className="max-w-md w-full">
        {/* Remove or update the animated background elements */}
        <div className="absolute inset-0 overflow-hidden">
          {/* Make the blobs more subtle to match dark theme */}
          <div className="absolute -top-40 -right-40 w-80 h-80 bg-blue-600 rounded-full mix-blend-multiply filter blur-3xl opacity-5 animate-blob"></div>
          <div className="absolute -bottom-40 -left-40 w-80 h-80 bg-gray-600 rounded-full mix-blend-multiply filter blur-3xl opacity-5 animate-blob animation-delay-2000"></div>
        </div>

        {/* Error Content */}
        <div className="relative">
          {/* Icon with Animation - update colors */}
          <div className="flex justify-center mb-8">
            <div className="relative">
              <div className="absolute inset-0 bg-blue-600 rounded-full blur-2xl opacity-10 animate-pulse"></div>
              <div className="relative bg-gray-800/50 backdrop-blur-sm rounded-full p-6 border border-gray-700/50">
                <Icon className="w-16 h-16 text-blue-500" />
              </div>
            </div>
          </div>

          {/* Error Code - keep the gradient but make it match */}
          <div className="text-center mb-4">
            <span className="text-6xl font-bold bg-gradient-to-r from-blue-500 to-blue-400 bg-clip-text text-transparent">
              {statusCode}
            </span>
          </div>

          {/* Title & Message - update background */}
          <div className="bg-gray-900/50 backdrop-blur-sm rounded-2xl p-8 border border-gray-800/50">
            <h1 className="text-2xl font-bold text-white text-center mb-4">
              {config.title}
            </h1>
            <p className="text-gray-400 text-center mb-8">
              {config.message}
            </p>

            {/* Action Buttons - match the home page button styles */}
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              {config.actionLink === null ? (
                <button
                  onClick={() => window.location.reload()}
                  className="flex items-center justify-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-all"
                >
                  <RefreshCw className="w-5 h-5" />
                  {config.action}
                </button>
              ) : (
                
                  href={config.actionLink}
                  className="flex items-center justify-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-all"
                >
                  {config.action}
                </a>
              )}
              
              
                href="/"
                className="flex items-center justify-center gap-2 px-6 py-3 bg-gray-800 hover:bg-gray-700 text-white rounded-lg transition-all border border-gray-700"
              >
                <Home className="w-5 h-5" />
                Home
              </a>
            </div>
          </div>

          {/* Additional Help Text */}
          <p className="text-center text-gray-500 text-sm mt-8">
            Error Code: {statusCode} | Need help? 
            <a href="/contact" className="text-blue-500 hover:text-blue-400 ml-1">
              Contact Support
            </a>
          </p>
        </div>
      </div>
    </div>
  );
}
Key Changes Made:

Background gradient: Changed from blue to match your home page's from-gray-900 via-gray-800 to-gray-900
Blob animations: Reduced opacity from opacity-10 to opacity-5 and changed colors to be more subtle
Card background: Changed from bg-gray-800/30 to bg-gray-900/50 to match the darker theme
Border colors: Updated to use border-gray-800/50 instead of border-gray-700/50
Button styles: Made the secondary button match your home page's style with bg-gray-800 and border-gray-700
Text colors: Kept the blue accent for links and primary actions to match your home page's blue "EQUIPMENT EXCHANGE" text

This will ensure all your error pages have the same professional, dark gradient background as your home page while maintaining the visual hierarchy and readability.