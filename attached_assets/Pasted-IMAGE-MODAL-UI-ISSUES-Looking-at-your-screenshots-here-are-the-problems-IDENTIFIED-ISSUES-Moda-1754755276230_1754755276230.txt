IMAGE MODAL UI ISSUES:
Looking at your screenshots, here are the problems:

IDENTIFIED ISSUES:

Modal Cut Off - Third image shows the modal going off-screen at the bottom
Inconsistent Centering - Modal position changes based on image dimensions
Navigation Arrows Misaligned - Arrow positions vary between images
Text Cut Off - "Click outside or press ESC to close" is cut off
No Max Height Constraint - Large images can overflow viewport


REPLIT INSTRUCTIONS TO FIX:
markdown## FIX IMAGE MODAL POSITIONING & OVERFLOW

### FILE: Find the image modal component (likely in ProductDetail or ImageViewer)

### FIXES:

#### 1. FIX MODAL CONTAINER POSITIONING
FIND the modal container (with `fixed inset-0`):
```jsx
<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/90">
REPLACE WITH:
jsx<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/90 p-4 md:p-8">
2. FIX IMAGE CONSTRAINTS
FIND the image element:
jsx<img 
  src={selectedImage} 
  alt="Product" 
  className="max-w-[90vw] max-h-[90vh] object-contain"
/>
REPLACE WITH:
jsx<img 
  src={selectedImage} 
  alt="Product" 
  className="max-w-full max-h-[80vh] w-auto h-auto object-contain"
  style={{ maxWidth: 'min(90vw, 1200px)' }}
/>
3. FIX NAVIGATION ARROWS POSITIONING
FIND arrow buttons and update positioning:
jsx{/* Previous Arrow */}
<button 
  className="absolute left-4 top-1/2 -translate-y-1/2 ..."
>

{/* Next Arrow */}
<button 
  className="absolute right-4 top-1/2 -translate-y-1/2 ..."
>
4. FIX BOTTOM TEXT POSITIONING
FIND the "Click outside" text:
jsx<div className="absolute -bottom-12 left-1/2 -translate-x-1/2 text-white/60 text-sm">
  Click outside or press ESC to close
</div>
REPLACE WITH:
jsx<div className="fixed bottom-4 left-1/2 -translate-x-1/2 text-white/60 text-sm z-50">
  Click outside or press ESC to close
</div>
5. COMPLETE FIXED STRUCTURE
jsx{selectedImage && (
  <div 
    className="fixed inset-0 z-50 bg-black/90 flex items-center justify-center cursor-pointer overflow-hidden"
    onClick={() => setSelectedImage(null)}
  >
    {/* Image Container - Prevents overflow */}
    <div 
      className="relative flex items-center justify-center w-full h-full p-8"
      onClick={(e) => e.stopPropagation()}
    >
      {/* Main Image */}
      <img 
        src={selectedImage} 
        alt="Product"
        className="max-w-full max-h-[85vh] w-auto h-auto object-contain rounded-lg"
      />
      
      {/* Navigation Arrows - Fixed position */}
      {currentIndex > 0 && (
        <button 
          onClick={handlePrevious}
          className="fixed left-4 md:left-8 top-1/2 -translate-y-1/2 ..."
        >
          <ChevronLeft />
        </button>
      )}
      
      {currentIndex < images.length - 1 && (
        <button 
          onClick={handleNext}
          className="fixed right-4 md:right-8 top-1/2 -translate-y-1/2 ..."
        >
          <ChevronRight />
        </button>
      )}
      
      {/* Close Button */}
      <button 
        onClick={() => setSelectedImage(null)}
        className="fixed top-4 right-4 ..."
      >
        <X />
      </button>
    </div>
    
    {/* Help Text - Always visible at bottom */}
    <div className="fixed bottom-8 left-1/2 -translate-x-1/2 text-white/60 text-sm pointer-events-none">
      Click outside or press ESC to close
    </div>
  </div>
)}
KEY FIXES:
✅ Added padding to prevent edge touching
✅ Limited max-height to 85vh to leave room for text
✅ Used fixed positioning for controls (not absolute)
✅ Added overflow-hidden to prevent scrolling
✅ Consistent positioning regardless of image size