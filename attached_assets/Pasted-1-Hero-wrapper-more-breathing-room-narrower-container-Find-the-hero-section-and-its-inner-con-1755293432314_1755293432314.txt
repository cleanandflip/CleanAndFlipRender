1) Hero wrapper (more breathing room, narrower container)

Find the hero <section> and its inner container:

<section className="flex items-center justify-center py-12 px-6">
  <div className="max-w-6xl mx-auto text-center">


Replace with:

<section className="flex items-center justify-center py-16 md:py-24 px-4 md:px-6">
  <div className="max-w-5xl mx-auto text-center">


Why: slightly more vertical padding and a narrower max width instantly de-clutters.

2) Headline block (tighter rhythm + smaller sizes)

Find this motion wrapper:

<motion.div className="mb-12" ...


Replace its class with:

<motion.div className="mb-10 md:mb-12" ...


Find the H1 + subhead + location text:

<h1 className="text-5xl md:text-7xl font-extrabold tracking-tight leading-tight">
  Local Weights, Fair Prices
</h1>

<p className="mt-3 text-lg md:text-xl text-muted-foreground">
  Buy & sell with fast pickup & delivery
</p>

<p className="mt-2 text-xs md:text-sm text-muted-foreground tracking-wide">
  Based in Asheville, NC
</p>


Replace ONLY the class strings (keep the text exactly):

<h1 className="text-4xl sm:text-5xl md:text-6xl font-extrabold tracking-tight leading-[1.08] max-w-3xl mx-auto">
  Local Weights, Fair Prices
</h1>

<p className="mt-4 text-base md:text-lg text-muted-foreground">
  Buy & sell with fast pickup & delivery
</p>

<p className="mt-3 text-xs md:text-sm text-muted-foreground tracking-wide">
  Based in Asheville, NC
</p>


Find the categories line below and just loosen spacing:

<p className="text-xl md:text-2xl max-w-3xl mx-auto mt-6 md:mt-8 font-semibold" ...


Ensure it reads:

<p className="text-lg md:text-xl max-w-3xl mx-auto mt-8 md:mt-10 font-semibold" ...

3) “Two Paths” cards (less padding, gentler gaps)

Find the grid wrapper:

<motion.div className="grid md:grid-cols-2 gap-8 mb-16" ...


Replace with:

<motion.div className="grid md:grid-cols-2 gap-6 md:gap-8 mt-8 md:mt-10 mb-12 md:mb-16" ...


Find each <Card ... className="p-10 text-center"> (there are two).

Replace p-10 with:

className="p-6 md:p-8 text-center"


Find the icon sizes inside each card (currently size={64}).

Replace both with:

size={48}


Why: the cards feel lighter and the icons stop crowding the header.

4) Trust bar (slightly tighter)

Find the trust grid:

<motion.div className="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-4xl mx-auto" ...


Replace with:

<motion.div className="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6 mt-4 md:mt-6 max-w-4xl mx-auto" ...


Why: smaller default gap + a bit of top margin helps separation from the cards.

5) “How It Works” section (remove full-screen height)

Find the section wrapper:

<section className="min-h-screen flex items-center py-12 px-6">


Replace with:

<section className="py-16 md:py-24 px-4 md:px-6">


Find the grid gap:

<div className="grid lg:grid-cols-2 gap-16 items-center">


Replace with:

<div className="grid lg:grid-cols-2 gap-12 md:gap-16 items-center">


Find the left column stack space:

<motion.div className="space-y-12" ...


Replace with:

<motion.div className="space-y-8 md:space-y-10" ...


Why: dropping min-h-screen and tightening gaps keeps this section from feeling heavy.

6) Categories section (minor)

Find the section wrapper:

<section className="py-24 px-6 spacing-section">


Replace with:

<section className="py-20 md:py-24 px-4 md:px-6 spacing-section">


Find the header wrapper:

<motion.div className="text-center mb-16" ...


Replace with:

<motion.div className="text-center mb-12 md:mb-16" ...

Quick acceptance checklist

Hero container is max-w-5xl, not stretching edge-to-edge.

H1 is one step smaller (md:text-6xl) with slightly looser line-height and a max-w-3xl line length.

Subhead and location lines are smaller with a hair more spacing.

Cards have p-6 / p-8 instead of p-10; icons are 48px.

Trust bar has a bit of top margin and slightly smaller gaps.

“How It Works” no longer forces a full screen height (less oppressive).

Category section has a bit less vertical padding and tighter header spacing.

This keeps your exact design and copy but gives the page the air and freshness it was missing.