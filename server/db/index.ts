// DEPRECATED: Using new environment isolation system
// import { createDatabaseConnection, validateDatabaseEnvironment } from '../config/database';

// Global database connection
let dbConnection: any = null;

// DEPRECATED: These functions are replaced by the main db.ts file
// The new environment isolation system handles database connections directly