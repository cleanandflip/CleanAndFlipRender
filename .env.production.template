# Production Environment Variables Template
# Copy this file to .env.production and fill in the values

# CRITICAL: Database Configuration
# Use the SAME database URL as Replit for consistency, OR ensure production database is properly set up
DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@YOUR_HOST.neon.tech/neondb?sslmode=require

# Email Service (Required for password reset)
RESEND_API_KEY=re_YOUR_RESEND_API_KEY

# Application Configuration
NODE_ENV=production
APP_URL=https://cleanandflip.com

# Image Storage (Optional but recommended)
CLOUDINARY_CLOUD_NAME=your_cloudinary_name
CLOUDINARY_API_KEY=your_cloudinary_key
CLOUDINARY_API_SECRET=your_cloudinary_secret

# Payment Processing (Optional)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key

# Redis Cache (Optional - improves performance)
REDIS_URL=redis://your-redis-host:6379

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here

# DEPLOYMENT INSTRUCTIONS:
#
# 1. Copy the DATABASE_URL from your working Replit environment
#    Run in Replit: echo $DATABASE_URL
#    Use the EXACT same URL in production
#
# 2. Set these environment variables in your deployment platform:
#    - Vercel: vercel env add [VARIABLE_NAME] production
#    - Railway: railway variables set [VARIABLE_NAME]="value"
#    - Render: Add in Environment settings
#    - Heroku: heroku config:set [VARIABLE_NAME]="value"
#
# 3. If you must use a separate production database:
#    - Run: npx tsx scripts/ensure-production-database.ts
#    - Or manually run: scripts/production-database-setup.sql
#
# 4. Test deployment with: GET /api/debug/database-info